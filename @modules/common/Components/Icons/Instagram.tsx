import { iconProps } from '@modules/utils/types';
import { BsInstagram } from 'react-icons/bs';

const Instagram: React.FC<iconProps> = ({ size }) => {
  return <BsInstagram size={size} />;
};

export default Instagram;
