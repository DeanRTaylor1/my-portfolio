import { BsGithub } from 'react-icons/bs';
import { iconProps } from '@modules/utils/types';

const Github: React.FC<iconProps> = ({ size }) => {
  return <BsGithub size={size} />;
};

export default Github;
